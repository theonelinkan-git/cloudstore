services:

  mysql:
    image: mysql:8.0
    container_name: mysql
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: [ "CMD", "mysqladmin", "ping", "-h", "localhost" ]
      interval: 10s
      timeout: 5s
      retries: 10
    networks:
      - cloudstore-net

  product-service:
    image: theonelinkan-git/cloudstore-product-service:latest
    container_name: product-service
    ports:
      - "8081:8080"
    environment:
      JWT_SECRET: ${JWT_SECRET}
    networks:
      - cloudstore-net

  user-order-service:
    image: theonelinkan-git/cloudstore-user-order-service:latest
    container_name: user-order-service
    ports:
      - "8080:8080"
    environment:
      PRODUCT_SERVICE_URL: ${PRODUCT_SERVICE_URL}
      SPRING_DATASOURCE_URL: ${SPRING_DATASOURCE_URL}
      SPRING_DATASOURCE_USERNAME: ${SPRING_DATASOURCE_USERNAME}
      SPRING_DATASOURCE_PASSWORD: ${SPRING_DATASOURCE_PASSWORD}
      JWT_SECRET: ${JWT_SECRET}
    depends_on:
      mysql:
        condition: service_healthy
      product-service:
        condition: service_started
    restart: on-failure
    networks:
      - cloudstore-net

  frontend:
    image: theonelinkan-git/cloudstore-frontend:latest
    container_name: frontend
    environment:
      VITE_API_URL: ${VITE_API_URL}
      VITE_PRODUCT_URL: ${VITE_PRODUCT_URL}
    ports:
      - "5173:5173"
    networks:
      - cloudstore-net

volumes:
  mysql_data:

networks:
  cloudstore-net: